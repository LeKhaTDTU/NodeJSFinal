<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Home</title>
    <link rel="stylesheet" href="/css/home.css">
    <script src="https://kit.fontawesome.com/ad1797946c.js" crossorigin="anonymous"></script>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Roboto:ital,wght@0,300;0,400;0,500;0,700;1,100;1,300&display=swap" rel="stylesheet">
</head>
<body>
    <!-- navgiation bar -->
    
    <%- include('navbar') %>
    <div class="container">
        
        <div class="container-bar">
            
            <div class="logout"><div><a href="/logout" class=""><i class="fa-solid fa-right-from-bracket"></i> Sign out</a></div></div>
        </div>
        
        <div class="dashboard-container">
            <div class="dashboard-content"></div>
            <div class="dashboard-content"></div>
            <div class="dashboard-content"></div>
            <div class="dashboard-content income-chart"></div>
            <div class="dashboard-content staff-list"></div>
        </div>

    </div>

</body>

<script>
    const product_management = document.querySelector('.product-management')
    const feature_list = document.querySelector('.product-management-features')
    const currentURL = window.location.href
    const link = document.getElementsByClassName('navbar')[0]
    console.log(currentURL)
    
    let count = 1
    function showOptions() {
        if(count % 2 == 0) {
            feature_list.style.display = 'none'
        }
        else {
            feature_list.style.display = 'block'
        }
        count++
    }
</script>
</html>